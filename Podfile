platform :ios, '6.1'

target :KiwiBaseTests, :exclusive => true do

    # to install from local path, uncomment line below
    # provide path to local cloned repo to the :path value
    pod 'Kiwi', :path => '/Users/tim/codeLibraries/Kiwi'

    # to install from public repo, uncomment line below
    # pod 'Kiwi'

end